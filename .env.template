# データベース接続設定
DB_HOST=localhost
DB_PORT=3306
DB_USER=user
DB_PASS=password
DB_NAME=hakopita_database_dev

# データベース設定（接続設定から自動生成）
DATABASE_URL=mysql+pymysql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# アプリケーション設定
APP_NAME=HakoPita FastAPI
DEBUG=false
LOG_LEVEL=INFO
